% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_check_versions.R
\name{pd_check_versions}
\alias{pd_check_versions}
\title{Check whether files in a PRISM archive are the latest release}
\usage{
pd_check_versions(pd = NULL, quiet = FALSE)
}
\arguments{
\item{pd}{A \code{pd} object as returned by \code{\link[=prism_archive_ls]{prism_archive_ls()}} or
\code{\link[=prism_archive_subset]{prism_archive_subset()}}. If \code{NULL} (the default), the full local
archive is used, i.e. \code{pd_check_versions()} is equivalent to
\code{pd_check_versions(prism_archive_ls())}.}

\item{quiet}{If \code{FALSE} (default), prints a message summarizing how many
files were queried against the web service.}
}
\value{
A data frame containing all the checked \code{pd}, some parsed information
about the pd, e.g., \code{data_class}, \code{time_step}, etc. plus a \code{status} column
with one of \code{"current"}, \code{"update_available"}, \code{"not_supported"}, or
\code{"check_failed"} for every row in \code{pd}.
}
\description{
PRISM continually revises recent monthly and daily grids as more station
data becomes available. Each local file's release number is stored in its
.info.txt file as the \code{PRISM_DATASET_RELEASE_NUMBER}. This
function compares that number against the release/grid count reported by
the PRISM release-date web service and flags files that have a newer
release available online.
}
\details{
Monthly data is considered "final" once \code{PRISM_DATASET_RELEASE_NUMBER >= 7}
and daily data once it is \verb{>= 8}; those files are not queried online (they
are marked \code{"current"} directly). Everything below that threshold is
checked against the web service. If the local file matches the latest
available, it is also marked \code{"current"}.

The release-date web service only supports monthly and daily time steps.
Annual time series and normals have no release-date endpoint, so rows
with \code{time_step == "annual"} or a normals \code{data_class} are never queried;
they are marked \code{"not_supported"} regardless of their release number,
since we have no way to confirm whether a newer version exists.
}
\examples{
\dontrun{
# Check everything already downloaded
status_df <- pd_check_versions()

# Check only a subset
pd <- prism_archive_subset("tmax", "monthly", years = 2015:2020)
status_df <- pd_check_versions(pd)
}

}
