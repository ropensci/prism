% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prism_archive_update.R
\name{prism_archive_update}
\alias{prism_archive_update}
\title{Update stale files in a PRISM archive}
\usage{
prism_archive_update(pd = NULL, keepZip = prism_get_keepZip(), quiet = FALSE)
}
\arguments{
\item{pd}{A \code{pd} object to check and update. If \code{NULL} (the default), the
full local archive is used via \code{\link[=prism_archive_ls]{prism_archive_ls()}}.}

\item{keepZip, quiet}{Passed through to \code{\link[=get_prism_pd]{get_prism_pd()}} /
\code{\link[=pd_check_versions]{pd_check_versions()}}.}
}
\value{
Invisibly returns the \code{pd} values that were actually re-downloaded
(an empty character vector if nothing needed updating).
}
\description{
\code{prism_archive_update()} checks release status via \code{\link[=pd_check_versions]{pd_check_versions()}} and
re-downloads any files flagged as \code{"update_available"}. It is essentially a
convenience wrapper for a user calling something along the lines of:

\if{html}{\out{<div class="sourceCode">}}\preformatted{x <- pd_check_versions()
pd_to_update <- x[["pd"]][x[["status"]] == "update_available"]
get_prism_pd(pd_to_update, overwrite = TRUE)
}\if{html}{\out{</div>}}
}
\details{
When run interactively, the user is given a choice on which \code{pd} to update.
When not interactive, all \code{pd} that are found that need updated, are updated.
}
\examples{
\dontrun{
# update the entire archive
prism_archive_update()

# update only precipitation data
pd <- prism_archive_subset(type = "ppt")
prism_archive_update(pd)
}

  
}
